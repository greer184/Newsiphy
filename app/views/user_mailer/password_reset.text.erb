Password Reset

<%= @user.name %>, use link below to reset password:

<%= edit_password_reset_url(@user.reset_token, email: @user.email) %>

This link will expire in two hours.

If you did not request your password to be reset, ignore this email.
